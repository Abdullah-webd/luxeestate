import express from 'express';

const app = express();

app.use(express.json());
app.listen(3000, () => {
  console.log('God bless u all pls u can all work on the backend now the frontend is done');
})